[project]
name = "neuralops-sdk"
version = "0.1.0"
description = "Tiny Python client for sending AI workflow traces to NeuralOps."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "NeuralOps" }]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

