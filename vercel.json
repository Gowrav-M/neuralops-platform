{
  "framework": "vite",
  "buildCommand": "cmd /c npm run build",
  "outputDirectory": "dist",
  "installCommand": "cmd /c npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
